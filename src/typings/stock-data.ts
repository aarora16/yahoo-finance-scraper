export interface StockData {
  name: string;
  symbol: string;
  currentPrice: string;
  dayChangeDollar: string;
  dayChangePercent: string;
  previousClosePrice: string;
  openPrice: string;
  dayRange: string;
  yearRange: string;
}
